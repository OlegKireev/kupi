{
  "name": "@kupi/server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "pretest": "tsx scripts/generate-db-types.ts --verify",
    "test": "node --import tsx --test src/**/*.test.ts",
    "db:generate-types": "tsx scripts/generate-db-types.ts",
    "db:verify-types": "tsx scripts/generate-db-types.ts --verify"
  },
  "dependencies": {
    "@fastify/cookie": "^11.0.2",
    "@kupi/shared": "workspace:*",
    "better-sqlite3": "^12.11.1",
    "fastify": "^5.9.0",
    "fastify-type-provider-zod": "^7.0.0",
    "kysely": "^0.29.2",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^26.0.1",
    "kysely-codegen": "^0.20.0"
  }
}
