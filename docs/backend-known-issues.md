# Backend — известный долг и находки ревью

Собрано по итогам реализации backend-плана (Tasks 1–8, коммиты `ea2017f..f71bb34`) и
двухстадийных + финального ревью. Ничего из списка не блокирует MVP; всё осознанно отложено.

## Находки ревью (не были в плане)

| #   | Где                                     | Суть                                                                                                                                                                                             | Что делать                                                                                                                                           | Приоритет |
| --- | --------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------- | --------- |
| 1   | `merge.ts` (`applied_ops`), `schema.ts` | Ключ идемпотентности `client_op_id` — глобальный PRIMARY KEY без скоупа списка. Безопасно, пока `clientOpId` глобально уникален (клиент генерит UUID), но контракт этого явно не требует.        | Либо задокументировать инвариант «clientOpId глобально уникален» в `ItemChangeSchema`/`applied_ops`, либо сменить ключ на `(list_id, client_op_id)`. | Important |
| 2   | `auth.ts` (onRequest)                   | Sliding-renewal куки выставляется в хуке до хендлера, поэтому TTL продлевается даже на запросах, которые хендлер потом отклоняет 404 (чужой список). Поведение безобидное.                       | Оставить, либо переносить `setAuthCookie` в `onSend` только для 2xx, если станет важно.                                                              | Minor     |
| 3   | `merge.ts` `bumpSeq`                    | UPDATE + отдельный SELECT на каждый `applyChange` (два стейтмента). При батчах в десятки изменений — лишние раунд-трипы. Не баг: better-sqlite3 кеширует prepared-стейтменты, батчи sync мелкие. | При нагрузочной работе — `UPDATE ... RETURNING seq` (SQLite 3.35+).                                                                                  | Minor     |

## Осознанно отложено (`// ponytail` в коде + раздел Deferred плана)

- **Очистка категории** (`categoryId: null`) трактуется как «без изменений» — `COALESCE` не отличает absent от explicit null. Нужен sentinel, если станет фичей. — `merge.ts`
- **modulo-bias в `newCode`** — на 32-символьном алфавите (256 % 32 == 0) смещения фактически нет; комментарий пессимистичен. — `ids.ts`
- **Purge sweep** для `applied_ops` и tombstones — растут монотонно. При заведении покрыть и `item_frequency`.
- **Token rotation / recovery**, QR для линковки, realtime-обновления, кастомные категории, name-dedup в suggestions, удаление списков.

См. также раздел «Notes for follow-up plans» в `docs/superpowers/plans/2026-06-30-backend.md`.
